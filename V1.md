# V1.md — the tiered path to v1 (supersedes CLAUDE.md §10's phase list for remaining work)

> Written 2026-07-05 after the post-Phase-8 review; **amended same day after a deep code
> review of the correctness core** (`app/ledger/`, `app/rules/`, `app/imports/`,
> `app/ingest/`, the wiring services, and dragonfly-id's smoke endpoint) — see "Code-review
> findings" below; each finding is folded into a tier. The phase list got the skeleton built
> and deployed; what remains is dependency-ordered, not sequential, so it's tiers from here.
> **A tier is a dependency bucket**: items within a tier can run in parallel / any order, but
> starting tier N+1 before tier N is done either wastes work or (Tier 0) violates a gate.
> `[H]` = human-gated (needs the owner's hands, credentials, or eyes — Claude can assist but
> not complete). Everything here inherits CLAUDE.md's locked decisions (§2) and guardrails
> (§6, §8) unchanged.
>
> **v1 is done when:** statement parity holds for two consecutive months (§9 gate), the owner
> runs the review queue daily from the phone without touching an API client, a simulated
> missing bill and a short paycheck both page the phone, and every screen passes the Tier 4
> UI/UX bar.

## Bootstrap — how the next session starts this work

1. **Read, in order:** host `C:\Code\CLAUDE.md` (suite invariants — signing key, epoch-minute
   versionCodes, compose `environment:` rule, no `pull_request` on self-hosted runners) →
   this repo's `CLAUDE.md` §2 (locked decisions — do not relitigate) → this file → the
   status header of `ARCHITECTURE.md` (as-built + known gaps).
2. **Verify live state before changing anything:** `docker ps` shows `magpie-server-1` /
   `magpie-db-1` up; `GET 127.0.0.1:8005/health` + `/version` (commit should match `main`);
   latest GitHub Actions runs green (check jobs, not the rollup); `git log origin/main..main`
   — **if local commits are unpushed, reconcile that with the owner first**, don't build on
   an undecided base.
3. **Respect the gates:** Tier 0 items are owner-authorized only. Specifically: no real
   financial data (backfill, IMAP creds) before the NAS-dump encryption lands; no dragonfly-id
   or Pulse or host-script change without a separate explicit go-ahead; new secrets in a live
   `.env` need their own confirmation even under a broad "go ahead".
4. **Start with Tier 1 in this order:** F1 (balance anchoring — pure math, well-specified,
   the parity gate depends on it) → F3+F12 (transfer guards + un-pair) → F6/F7/F8/F16
   (rule/parser fixes) → seeded categories (migration) → review-queue correct flow + Settings
   category editor (Android). **Each F-fix begins with a regression test that reproduces the
   bug** — the finding's "failure scenario" column is the test case.
5. **Work discipline (proven across Phases 0–8):** server tests against a throwaway DB
   (`127.0.0.1`, never localhost; `DB_NULLPOOL`); `ruff check` AND `ruff format --check`
   before pushing; Android `testDebugUnitTest` + `assembleDebug` locally; ARCHITECTURE.md
   updated **in the same commit** as the code; after push, verify CI job-by-job, then confirm
   the auto-deploy via `/version`. Public repo: sentinel-only fixtures, no personal PII —
   when a scanner flags one file, grep the rest of the commit for the same pattern.
6. **Multi-agent note:** if parallelizing, each agent works in its own worktree off `main`
   (sibling directory + copy `local.properties`), never the shared checkout.

## Code-review findings (2026-07-05) — severity-ranked, each mapped to a tier

> **Progress (2026-07-08):** the Tier 1 backend correctness core is landed —
> **F1, F3, F12, F6, F7, F16, F8, F4 fixed** and the shared-category seed migration (Tier 1 #8)
> shipped, each with a regression test that reproduces the finding's failure scenario and
> ARCHITECTURE.md updated in the same commit. **#10's backend gap is also closed:**
> `PATCH /categories/{id}` (rename own; shared read-only) + the matching Android Retrofit
> bindings, so the Settings category editor has full CRUD to bind to.
> **Update 2026-07-08 (later, on the Dragonfly host — full Android toolchain available):** the
> two Compose-only items are **done** — the review-queue "correct" flow (#9, `CorrectionSheet`:
> category picker + kind correction, two-tap max) and the Settings category-editor UI (#10, new
> `ui/settings/`), each with recorded+verified Roborazzi baselines (light+dark) eyeballed by a
> human per the wrapped-money lesson. `assembleDebug` + `testDebugUnitTest` green. **The only
> Tier 1 item left is IMAP credentials `[H]` (#11)** — purely owner-gated (Gmail app password +
> forwarding verification + set `imap_*` in compose `environment:`); the server poller/config
> already exist, so no code is owed. Tier 0 gates and Tier 2+ untouched.

| # | Sev | Finding | Fix in |
|---|-----|---------|--------|
| F1 | **Critical** | **Balance/checkpoint math can't reconcile.** `account_service.compute_account_balance` sums *every* transaction ever and compares against the latest checkpoint's stated balance. Unless the ledger holds the account's complete lifetime history from $0, the delta is never zero — after a 12-month backfill every account reads "Off by $X" (X = the balance 12 months ago) forever, and the statement-parity acceptance gate is mathematically unreachable. Spec §4 says balance is computed *between* checkpoints: derived = earliest checkpoint's stated balance + signed sum of transactions dated after it (and the delta compares that at the latest checkpoint). | Tier 1 |
| F2 | **Critical (hardening)** | **Pin the smoke-token subject email.** dragonfly-id's `POST /smoke/token` accepts a caller-supplied `subject_email`; §9's original design called for "a designated throwaway smoke email". Add an allowlist (e.g. `SMOKE_SUBJECT_EMAILS`) in dragonfly-id config and reject others, per least-privilege. Shared prod identity server — needs its own go-ahead. | Tier 0 |
| F3 | **High** | **Transfer matching pairs any ±equal amounts.** `_find_transfer_partner` matches on exact opposite amount + different account + 3-day window with **no kind/status/review-state/merchant guard** — a $50 card spend and a $50 Zelle deposit into checking become a "transfer", both silently rewritten to `review_state="auto"` and excluded from all rollups; the partner is mutated **even if it was human-confirmed**. Round-amount collisions are common in real data. Guards needed: payment-shaped signals (merchant keywords / kind expectations), never silently flip a `confirmed` row (route to review instead), and an un-pair action (F12). | Tier 1 |
| F4 | **High** | **Rollups will double-count the moment email + CSV both run.** Email rows stay `status="pending"` forever, CSV creates independent `posted` rows, and `rollup_month`/`list_transactions` have no status logic — every emailed swipe counts twice in the Home panel after each reconciliation. Same root as pending→posted matching (Tier 1 #7); named separately because it corrupts the headline numbers, not just the row list. | Tier 1 |
| F5 | **High** | **Card CSV sign inversion is near-certain at backfill.** Amex (notoriously) exports charges as *positive* amounts; the generic parser + `default_kind` from sign will import an entire card history inverted (charges as income). The spec's "per-institution column mappings" were never built — add a per-import (or per-account-institution) mapping incl. a sign-flip flag, validated against the real files. | Tier 2 |
| F6 | **Medium** | **Recurring rules permanently derail after one miss.** `last_matched_at` advances only on a successful auto-file; a single out-of-band/missed occurrence routes to review, the human confirms, but the rule's window never advances — every subsequent occurrence is "outside expected cadence window" forever. Also `last_matched_at` is set to wall-clock `now`, not the matched transaction's date, skewing windows for delayed imports. Fix: confirming a rule-flagged transaction advances its rule; store the txn date. | Tier 1 |
| F7 | **Medium** | **Zelle direction unguarded.** `parse_usbank` treats any "Zelle payment" subject as `income` with a positive amount — a "You sent a Zelle payment" alert would book outbound money as income. Require the received/deposited template explicitly; parse "sent" as spend or raise `UnparsedEmail`. | Tier 1 |
| F8 | **Medium** | **Merchant normalization strips bare prefixes.** `^(SQ\|TST\|PAYPAL\|SP\|POS\|AMZN\|WL)\s*\*?\s*` has all-optional separators, so it fires mid-word: SPOTIFY→"OTIFY", POSTAL SERVICE→"TAL SERVICE", POSTMATES→"TMATES". Combined with two-way substring containment (observed "AMAZON" matches rule "AMAZON PRIME"), rules will mis-fire across unrelated merchants. Require a real separator after the prefix; consider one-way containment. | Tier 1 |
| F9 | **Medium** | **CSV dedupe collapses legitimate duplicates.** The (account, date, amount, description) fingerprint skips the *second* identical same-day transaction (two $5.00 coffees) — real rows vanish and parity fails confusingly. Make dedupe multiplicity-aware (count occurrences in file vs. DB). | Tier 2 |
| F10 | **Medium** | **Budgets are global, not user-scoped.** `Budget` has no `user_id` and `list_budgets` doesn't filter — deliberate "single-user" shortcut, except the smoke client's throwaway user **is a second user in prod**, able to read/write the owner's budgets. Scope by user (or restrict budget routes to the primary user) before Budgets gets a UI. | Tier 3 |
| F11 | **Low** | **Alert latch is process memory, not data** (§9 says "the latch state is data") — every redeploy with a nonzero unparsed backlog re-pages. Persist per-alert latch state. | Tier 3 |
| F12 | **Low** | **No un-pair / correction path for transfer legs**, and `PATCH` kind-changes don't touch `transfer_group` — correcting one leg of a bad pair leaves a dangling group that violates the zero-sum invariant. Needed alongside F3's guards. | Tier 1 |
| F13 | **Low** | **Bill matching can bind to the wrong row**: pool is all transactions on the account, matched on `abs(amount)` — a same-magnitude deposit/refund can "pay" a bill, and two same-amount bills can claim one payment. Filter by sign/kind; enforce one bill per matched transaction. | Tier 3 |
| F14 | **Low** | **Scale/perf**: `_observation_history` loads the whole account table per evaluated row (O(N²) across a backfill, with regex normalization per comparison); `GET /transactions` has no pagination. Add SQL-side filtering + limit/offset before the backfill makes both hurt. | Tier 2 |
| F15 | **Low** | **Parser-replay is a column, not a capability** — `raw_payload` + `parse_version` are stored but no endpoint/script replays history through a fixed parser. Build the replay tool when the first parser fix needs it (or in Tier 3 with the bill parser). | Tier 3 |
| F16 | **Low** | **`_match_account` crashes the poll on last4 collision** (`scalar_one_or_none` raises on two accounts sharing last4), aborting the whole batch. Degrade to unparsed instead. | Tier 1 |
| F17 | ~~**Low**~~ **DONE 2026-07-09.** `TokenStore` now stores the JWT pair in `EncryptedSharedPreferences` (`magpie_auth_secure`, AES256-GCM master key) — the suite `PatStore` pattern — instead of plaintext DataStore. The reactive `accessToken`/`refreshToken` Flows are preserved as `StateFlow`s seeded off the main thread; a read/decrypt failure → null → signed-out → re-SSO (never a lockout). The old plaintext `magpie_auth` DataStore is purged on first run (existing users re-authenticate once). Built + assembles; **needs an on-device pass** (Keystore can't run in JVM tests). | Tier 4 |
| F18 | **Audit** | **Timezone/month-boundary semantics unaudited**: transaction dates come from parsers/CSV, "this month" comes from client-supplied year/month, `now()` is UTC — a late-evening swipe near month end may file under the wrong month vs. the owner's local time. One pass to pick and document the convention. | Tier 2 |

## Tier 0 — Gates (nothing real enters the DB before these)

1. **✅ DONE (2026-07-08) — Encrypt the NAS DB dumps** (host ROADMAP2 Tier 1 #10).
   `Backup-DragonflyDatabases.ps1` now gpg-encrypts every dump (AES-256, symmetric) on the host
   before it touches `\\Diskstation` → `magpie-20260708-...dump.gpg`; **fail-closed** (missing
   gpg/passphrase aborts, never writes plaintext). Passphrase in `~\.dragonfly-suite\db-backup.gpg.pass`
   (SECRET-INVENTORY.md, recovery-critical). `Test-DragonflyDbRestore.ps1` decrypts-then-restores and
   was extended to include magpie — all five stacks PASS with exact row parity. The pre-existing
   plaintext dumps were re-encrypted in place + plaintext deleted (0 plaintext remaining). **The
   backfill gate is now open.** _User action owed: passphrase → password manager + offsite copy._
2. **✅ DONE (2026-07-08) — Pin the smoke-token subject email on dragonfly-id (F2).** Added a
   `SMOKE_SUBJECT_EMAILS` allowlist (fail-closed — empty denies everyone), checked after client
   auth so it isn't an unauthenticated oracle; a valid smoke credential now mints only for a
   pre-designated throwaway email, else 403. Pinned to `magpie-smoke@dragonflymedia.org` in
   dragonfly-id's compose `environment:` (invariant #4). Shipped + **deployed to the live identity
   server** (commit `1630ad8`, verified in-container + endpoint healthy); 3 new tests, suite green.
3. **Prove on-device SSO sign-in end-to-end.** `[H]` The `magpie` client is registered on
   dragonfly-id and `SuiteAuthManager` is wired, but "Sign in with Dragonfly" has never been
   exercised on the phone. Install the released APK via the hub, sign in, confirm a token
   lands and `/transactions` loads. Everything below assumes this works.

## Tier 1 — Make review real + fix the correctness core (currently inoperable and unsafe to feed)

4. **✅ DONE (2026-07-08) — Fix the balance/checkpoint anchoring (F1).** Derived balance anchors at the earliest
   checkpoint's stated balance and sums forward; delta compares at the latest checkpoint.
   Without this the Accounts screen's honesty meter lies and the v1 gate can't pass. Add the
   invariant test the spec implies: backfill-then-checkpoint on a synthetic account with
   unknown prior history reconciles to zero.
5. **✅ DONE (2026-07-08) — Guard transfer matching (F3, F12).** Payment-shaped signals before pairing; a match
   against a `confirmed` row routes to review instead of silently rewriting it; an un-pair
   action (API + review-queue affordance) that dissolves the group cleanly; `PATCH` kind
   changes clear/validate `transfer_group`.
6. **✅ DONE (2026-07-08) — Rules that survive a miss (F6)** — confirming a rule-flagged transaction advances the
   rule's `last_matched_at` (to the transaction's date, not wall-clock).
7. **✅ DONE (2026-07-08) — Parser direction + resilience (F7, F16, F8).** Zelle sent-vs-received guard; last4
   collisions degrade to `unparsed` instead of aborting the poll; fix the noise-prefix regex
   (require a separator) and tighten merchant containment.
8. **✅ DONE (2026-07-08) — Seed categories.** New migration inserting the shared set (`user_id NULL` — the model
   already supports it): groceries, dining, transport/gas, utilities, housing, subscriptions,
   entertainment, health, shopping, travel, cash, income, other. Without a vocabulary the AI
   stage never fires and every confirm files uncategorized spend.
9. **✅ DONE (2026-07-08) — Review queue: the "correct" half of approve/correct.** Each row gets a
   tonal "Correct" action opening a `ModalBottomSheet` (`CorrectionSheet`): the full category
   list + a kind selector for the rare sign-ambiguous case. Tapping a category confirms
   immediately, so the happy path stays one tap and a correction is ≤2. "Accept AI suggestion"
   stays the one-tap happy path. All paths route through one `confirm(id, categoryId?, kind?)`;
   the server's sign re-validation surfaces as an error rather than corrupting the ledger.
10. **✅ DONE (2026-07-08) — Settings screen v1: category editor.** New `ui/settings/`: list
    categories, rename/delete own (shared ones show a read-only "Shared" label — the server 404s
    their mutation), add, plus an About block with the server `/version`. Rules editor is Tier 3.
11. **✅ DONE (2026-07-08) — IMAP credentials + the live-swipe proof.** Routing changed to
    main-account IMAP scoped to the `magpie-ingest` label (forwarding abandoned — Google's
    anti-automation wall). Owner generated a main-account app password (`~\.dragonfly-suite\
    magpie-main-imap.txt`); wired as `IMAP_PASSWORD` in `server/.env` + `INGEST_USER_EMAIL`, with
    `MAGPIE_IMAP_HOST`/`MAGPIE_IMAP_USER` in the host root `.env` (compose substitution keeps the
    email out of this public repo). **Live-swipe proof met:** the poller connected to Gmail and the
    Amex parser parsed 22 real alerts (`parser=amex`) on the first poll. They currently land
    `outcome=unparsed` only because the owner hasn't created the real accounts (last4s) yet — a
    "Test" account is all that exists; once the Amex/US Bank/Discover accounts exist,
    new alerts auto-file. **This completes Tier 1.** _(No replay tool yet for the 22 already-seen —
    F15; harmless, they're the last 3 days and CSV backfill brings the real history.)_
12. **✅ DONE (2026-07-08) — Pending→posted matching (F4).** Email pending + CSV posted for the same swipe merge
    into one row (account + amount + date window; tip-adjust tolerance), and rollups treat
    the merged row once. **Must land before Tier 2's backfill meets live email ingestion.**

## Tier 2 — Real data + truth (the product has never processed a real dollar)

13. **~~Per-institution import mappings (F5).~~ SIGN-FLIP DONE (2026-07-08); validation + UI
    override are follow-ups.** New `app/imports/institution_mappings.py`: `resolve_sign_flip(
    institution, override)` — `import_csv` flips every row's sign when the institution's known
    convention (or an explicit per-import override) says so, before the sign→kind derivation.
    **Amex** is registered as positive-is-charge (substring match, so "Amex Gold" resolves);
    unknown/depository default to no flip. Regression test: an Amex positive charge now imports as
    a negative **spend**, a payment as an inflow. **Still to do:** (a) confirm **Discover**'s
    convention against a real export (a card — almost certainly needs the flip too, but left out
    per the don't-guess rule) during #14; (b) surface the `flip_sign` override as an import-dialog
    checkbox (server param exists, Android UI not wired); (c) column-alias overrides remain generic
    in `csv_parser` — only add per-issuer ones if a real file needs them.
14. **12-month CSV backfill.** `[H]` Owner downloads per-account history (Amex, Discover,
    US Bank checking; **decide whether the Visa is in scope for v1** — it was never checked
    in Phase −1 and no decision is recorded); import via the Accounts screen. Expect parser
    reality-fixes (that's the point). Seeds the rules engine's medians (cold-start §5).
15. **~~Dedupe multiplicity (F9).~~ DONE (2026-07-08).** Import dedup now counts occurrences per
    `(date, amount, description)` fingerprint (snapshot once per import + a `consumed` counter)
    instead of a boolean "does any match exist": two genuinely distinct same-day duplicates both
    survive, a partial re-import adds only the new occurrence, and re-import stays idempotent. Also
    fixes a latent crash — `scalar_one_or_none` *raised* on re-import once two identical rows
    existed. Three tests in `test_imports.py`.
16. **Statement-parity clock starts.** Anchor checkpoints, drive every per-account delta to
    "Reconciled" (possible once F1 is fixed), start counting the two consecutive
    to-the-penny months (§9).
17. **A paycheck ingestion path.** `[H]` Confirm whether US Bank deposit alerts actually
    send email (real sample first, Phase −1 discipline); parser if yes, else the paycheck
    rule keys off CSV history alone and the doc says so.
18. **~~Scale pass (F14).~~ SERVER-SIDE DONE (2026-07-08); Android infinite-scroll is Tier 4.**
    `_observation_history` no longer loads + Python-normalizes the *entire* account table per
    evaluated row (the O(N²) across a backfill) — the merchant substring is prefiltered in SQL via
    `merchant_norm ILIKE %matcher%` (both are stored already-normalized), with the exact one-way
    `matches()` still the Python authority (the ILIKE is a broadening superset, never drops a real
    match). `GET /transactions` gains opt-in `limit` (≤500) / `offset` params (default unbounded, so
    the review queue + existing callers are unchanged); the client that consumes them is Tier 4 #32.
    _Not done: a trigram/GIN index on `merchant_norm` — the ILIKE is still a seq scan, but the DB
    now does the filter instead of Python; add the index only if profiling on real backfill needs it._
19. **~~Timezone/month-boundary audit (F18).~~ DONE (2026-07-08).** Convention: months are
    **owner-local**. The one tz-sensitive spot was the email fallback date (`event_date or
    received_at.date()` — UTC), which rolled a late-evening month-end swipe into the next month.
    New `app/time_util.owner_local_date(dt, tz)` converts the timestamp to `settings.owner_timezone`
    (default `America/Chicago`, IANA-configurable) before taking the date; `tzdata` pinned so the
    slim image has the zone db. CSV/manual dates arrive as real `date`s and are untouched; the
    monthly rollup is client-driven (owner-local phone). 4 boundary tests.
20. **Coverage matrix as an artifact:** one table in ARCHITECTURE.md — per account: email
    alerts? CSV? paycheck path? — and check it against ROADMAP.md's SimpleFIN triggers
    (Discover email-less + Visa undecided may already be adjacent to the trigger).

## Tier 3 — The pitch features (what the owner actually bought)

21. **The four missing sweeps**, all latched, all via ntfy. **DONE 2026-07-08: the F11 persisted
    latch + missing-bill sweep** — the spec's "a simulated missing bill pages the phone" exit is
    **met**. New `alert_latches` table + `alert_latch_service.latched_should_alert` (migration
    `d5f2a1b3c4e6`): the "already alerted?" bit is now DATA keyed per condition, so a redeploy no
    longer re-pages a still-open condition (the old sweep held it in process memory). The existing
    unparsed-backlog sweep was converted to it too. `run_missing_bill_sweep` pages once per overdue
    unmatched bill (owner-local due-date grace, F18); 8 time-travel tests. **Also DONE
    2026-07-08: paycheck-late + per-account-freshness sweeps.** `run_paycheck_late_sweep` — a
    recurring-income rule whose expected paycheck (from cadence + `last_matched_at`) is overdue
    past its slack pages once, and clears when F6 advances the rule on arrival.
    `run_account_freshness_sweep` — an account with prior email-alert activity but none in
    `account_freshness_days` (default 14) pages once (silent alert-decay); no-history accounts are
    skipped so a new/manual-only account never false-alarms. Both latched + time-travel-tested.
    **Still to build:** **auth-hold expiry** (~7-day unmatched pendings auto-drop with an audit
    note — a data-*mutation* sweep, structurally different) and **paycheck-*short*** (band-based —
    better detected at ingestion than in a sweep). Plus sign/kind filters + one-payment-per-bill in
    bill matching (F13).
22. **Rules editor + "make this a rule".** **DONE 2026-07-08.** Editor: `ui/rules/` (`RulesScreen`
    + pure `RulesContent` + `RulesViewModel`) on the existing Phase-5 rules CRUD — lists every rule
    with its matcher, a human type label (Income/Bill/Transfer/Category rule) and a one-line summary
    (cadence `kind ±Nd`, band `±N%`, `→ Category`), an enable/disable `Switch` (optimistic →
    `PATCH /rules/{id}`), delete behind a confirm dialog (`DELETE /rules/{id}`); linked from Home;
    light+dark Roborazzi baselines. Growth loop: the review-queue `CorrectionSheet` gained an
    "Always file '<merchant>' this way" checkbox — ticking it then picking a category creates a
    `merchant_category` rule (`POST /rules`, matcher = the txn merchant, server-normalized) *after*
    the confirm, so future transactions from that merchant auto-file and skip the queue.
    **Still to build:** inline editing of a rule's band/cadence (today enable/disable + delete only).
23. **Cash-flow calendar — "due before next paycheck".** The headline screen of the pitch.
    **DONE 2026-07-08 (backend + Android).** Backend: `GET /cashflow` (`app/routers/cashflow.py`
    → `cashflow_service` → pure `app/rules/cashflow.py`) projects the next paycheck (soonest
    `expected_next_date` across the recurring-income rules) and returns the account's unmatched
    upcoming bills (recent-overdue within a 30-day past window through the future), each flagged
    `is_overdue` + `before_next_paycheck`, plus `total_due_before_paycheck_cents`; owner-local
    dates (F18); 6 tests. Android: `ui/cashflow/` (`CashflowScreen` + pure `CashflowContent` +
    `CashflowViewModel`) — a paycheck header (next date + total due before it) over the bills split
    into "Due before next paycheck" / "After payday" sections, corrected color grammar (red only
    for overdue, amber for the before-payday set); linked from Home; light+dark Roborazzi baselines.
    **Enhancements deferred:** recurring-*bill* rule projections into the upcoming set (today it's
    concrete `bill_statements` only); paycheck-*short* fidelity once #17's paycheck path is
    confirmed.
24. **`bill_issued` email parser + the replay tool (F15).** `[H]` Confirm Discover's real
    statement-ready sender, build from the real corpus; build the `ingest_events` replay
    script alongside (first real use case for it).
25. **Budgets screen** — **DONE 2026-07-08 (incl. F10).** First, F10: added `budgets.user_id`
    (migration `e7c1a9d4f0b2`, additive/nullable), `create_budget` sets it, `list_budgets(user_id,
    month)` filters it, router passes `current_user.id`; a cross-user scoping test. Then the screen:
    `ui/budgets/` (`BudgetsScreen` + pure `BudgetsContent` + `BudgetsViewModel`) — month-vs-budget
    by category for the current month (`GET /budgets?month=`, category names from `GET /categories`),
    each row an actual-vs-budget `LinearProgressIndicator` with "left"/"over by", over-budget in the
    red channel and under in green; a FAB opens an add-budget dialog (category picker + amount →
    `POST /budgets`); linked from Home; light+dark Roborazzi baselines. Feeds Home's month panel
    (#27) later.
26. **Transaction splits + correction flows.** **BACKEND DONE 2026-07-08; UI folds into Tier 4
    #32.** Splits: new `transactions.is_split`/`split_parent_id` (migration `f3b8c2e9a1d7`);
    `POST /transactions/{id}/split` (parts must sum to the amount, ≥2 parts, sign-valid, not a
    transfer/already-split) creates child allocations and marks the parent; `DELETE
    …/split` unsplits. The **no-double-count invariant** is enforced by filtering: the parent
    stays the ledger row (balance/list/month totals via `split_parent_id IS NULL`) and the parts
    carry the category breakdown (budget rollup via `NOT is_split`) — 7 tests prove month total +
    balance are unchanged by a split while budget actuals use the parts (groceries + cash-back).
    Recategorize-a-confirmed-row and edit/delete manual entries are already backed by
    `PATCH`/`DELETE /transactions/{id}`. **Split allocator UI DONE 2026-07-09** (`SplitSheet` on the
    Transactions screen — see #32; interactive pass owed on-device). Recategorize + edit/delete manual
    entries still to come with the rest of #32.

## Tier 4 — The Spotter/Plate parity pass (UI/UX)

> **The bar, stated once (from the 2026-07-05 suite comparison):** Magpie's screens are
> currently component assemblies at roughly Spotter's pre-Sprint-5 fidelity, while
> Spotter/Plate/Cookbook are designed surfaces — hero greeting panel with the app's
> gradient and a live status line, a bottom nav shell, stat tiles whose values fit,
> content (not links) on Home, and designed empty/error states. "Done" for this tier =
> put Magpie's Home next to Plate's and it reads as the same family.

27. ~~**Adopt the suite bottom bar.**~~ **DONE 2026-07-08 (mirrors `CookbookBottomBar`).** New
    `TopLevelDestination` (Home · Activity[Transactions] · Bills · Budgets · Settings) +
    `MagpieBottomBar` (Material3 `NavigationBar`, teal money channel for selection). `MagpieNavHost`
    now wraps the graph in a `Scaffold` whose bottom bar shows only on top-level routes, with
    Cookbook's `goTab` save/restore navigation; the tab screens dropped their back buttons (they're
    tabs, not pushed), while secondary screens (Accounts, Review queue, Cash flow, Rules, Cash entry)
    are pushed and keep theirs. Home dropped its four tab-redundant nav buttons (Review queue / Cash
    flow / Accounts / Rules links remain until #29 turns them into content). Bottom-bar + affected
    Roborazzi baselines recorded + eyeballed. (Vestigial `onBack` params on the tab `*Content`s get
    swept in #29.)
28. ~~**Hero panel, using the gradient the app already owns.**~~ **DONE 2026-07-08.** Home now
    opens with `MagpieHero`: the indigo→teal→green `MagpieTheme.colors.heroGradient` (added to Pulse
    for this app and until now used by no screen), a time-of-day greeting, and the money status
    sentence "$3,180 net this month · 3 to review · XCEL ENERGY due Jul 18". `HomeViewModel` resolves
    the greeting (so the pure Content stays deterministic for screenshots) and best-effort-loads the
    review count + soonest upcoming bill (`GET /transactions?review_state=needs_review`,
    `GET /cashflow`) for the line. Home Roborazzi baselines re-recorded + eyeballed. (The button list
    below the hero is still there until #27 bottom nav + #29 content land.)
29. ~~**Home becomes content, not links.**~~ **DONE 2026-07-09 (mirrors Plate's Home cards).**
    Home's link-buttons are now live content: `ReviewQueueCard` (the count as a big amber datum →
    tap to the queue) + `UpcomingBillCard` (the soonest bill's biller/amount/due, teal → tap to the
    cash-flow calendar), over the month panel + hero; only the two non-tab utilities (Accounts,
    Rules) remain as compact secondary links. FAB stays cash entry. Both cards are Plate-style
    tappable `PanelCard`s. Home Roborazzi baselines re-recorded + eyeballed. (Fuller previews — top
    review rows, a multi-bill strip — can grow later; the count + next-bill are the load-bearing
    signals.)
30. ~~**Fix the StatTile money wrapping.**~~ **DONE 2026-07-08 (local, no Pulse change).** The
    standard `StatTile` renders its value in Pulse's large `dataMedium`, which wrapped even `$0.00`
    across three lines in a 1/3-width column. Fixed locally in Magpie's `MonthPanel`: switched to
    Pulse's existing `dense` metric tile (`dataSmall`, the layout Spotter already uses) + a new
    `formatCentsCompact` (whole-dollar values — `$4,500`, cents reserved for detail views). No Pulse
    edit needed (a cross-repo change per Pulse/CLAUDE.md), and it nudges Home toward the sibling
    metric-row look. Home Roborazzi baselines re-recorded + eyeballed.
31. **Color grammar corrections (from the 2026-07-05 color critique).** **Red overuse + Bills
    amount DONE 2026-07-09; the teal-overload/AI-violet refinement is the remaining bit.** Fixed:
    ordinary spend is now neutral, not red — the Transactions row, the Review-queue row amount, and
    the Home SPEND tile all render neutral (only income keeps green); a normal (negative) card
    balance on Accounts is neutral, with the reconciled/off-by delta carrying the signal; Bills
    colors the status *word* (Missing/Paid/Awaiting), never the amount. Red now appears only where
    it's a real deviation (over-budget, overdue/missing bills). Eyeballed the review queue + Home
    (red is no longer the dominant color). **Still to do (smaller):** give AI-suggestion text its
    own voice (violet, unclaimed) instead of teal, and pare teal back to brand/primary + money-total
    only. The theme layer is right (four channels from proven Pulse hues); the remaining fixes are
    usage:
    - **Red is over-loaded and dominates.** `overBudget` is documented as
      "over-budget/out-of-band deviations" but is used for every spend row (Transactions,
      review queue, Home spend tile) and every negative account balance — a credit card's
      normal statement balance renders as an alarm, and in a finance app ~80% of rows are
      spend, so the *rarest-should-be* color is the app's dominant one. Siblings reserve
      red for genuine errors. **Rule: ordinary spend and normal card balances are neutral
      (default/dim foreground); red appears only for over-budget, out-of-band, and missing
      states.**
    - **Bills colors the amount by status** (a paid bill's −$45 renders *green*, breaking
      the sign grammar the rest of the app teaches). Color the status word
      (Paid/Missing/Awaiting), never the amount.
    - **Teal means four unrelated things** — primary action, net/transfer, "awaiting
      payment", and AI-suggestion text — while the lead accent barely appears otherwise
      (no hero, no section identity). Plate's discipline is one hue = one domain. **Rule:
      teal = brand/primary + money-total (net, balances-in-good-standing); give AI drafts
      their own voice (violet is unclaimed inside Magpie and already reads
      "intelligence/system" in the suite); "awaiting" is neutral, not teal.**
    - **Channel density per card:** a review card currently carries amber border + red
      amount + amber note + teal AI text + teal button — four hues. Siblings hold cards to
      1–2 channels; the card's *context* channel (amber) plus at most one datum channel.
    - Keep: green = income/under-budget/reconciled (consistent today), amber =
      needs-review/warning, the local red channel built from `PulseRed`/`PulseRedDeep`
      rather than invented hues (that part was done right).
32. **Transactions grows up.** **CORE DONE 2026-07-09 (category-per-row + filter chips, mirrors
    Plate's `FilterChip`).** `TransactionsContent` (now split out + screenshot-covered) shows the
    **category name per row** (from `GET /categories`) and a chip row — All / Needs review / Spend /
    Income — filtered client-side; income keeps green, spend neutral (#31 grammar). **Split UI DONE
    2026-07-09 (#26's correction surface, part 1):** tapping a non-split row opens `SplitSheet` (a
    `ModalBottomSheet` over the pure, screenshot-tested `SplitSheetContent` allocator) — parts of
    category + amount, live "Remaining ${delta}" / "Fully allocated", Add-part / remove-part, Split
    enabled only when parts sum to the total (≥2, all categorized) → `POST /transactions/{id}/split`,
    then reload; split parents show a "Split" tag and aren't re-splittable. `is_split` added to the
    Android `TransactionOut`; parts carry the parent's sign so the user types positive dollars. Layout
    Roborazzi'd (`split_sheet`); **the interactive allocation — typing amounts, category picker, submit
    — needs an on-device pass (like F17), since Roborazzi only covers the static layout.** **Still to
    do (needs real transaction volume to build/test against meaningfully):** account/category/date-range
    filters + text search + infinite scroll against #18's pagination; and the rest of #26's correction
    surface — recategorize a confirmed row (`PATCH`), edit/delete manual entries (`DELETE`).
33. **Onboarding + state-handling consistency to Plate's standard.** **Refresh + designed states
    IN PROGRESS.** Done 2026-07-09: **refresh-on-resume** on all eight VM-wired screens (Cookbook's
    `ON_RESUME → load()` — replaces pull-to-refresh, which the suite doesn't use); **designed
    empty/error states** via Pulse's `EmptyState(icon, title, subtitle)` (matching Plate) on the
    high-traffic screens — Transactions (empty / filtered-empty / error), Review queue ("All caught
    up"), Accounts ("No accounts yet"), **and now Bills / Budgets / Rules / Home-error too** — the
    designed `EmptyState` is on every screen (Cash flow's "nothing due" stays an inline line under
    the paycheck header, by design). **Still to do:** a guided first-run (sign in → accounts →
    import → budgets) and loading placeholders. (No retry button / pull-to-refresh — the suite
    relies on resume-refresh, now in place.)
34. **Alert deep links:** an ntfy page opens the relevant screen (review queue, bill, the
    unparsed-events view), not just the app.
35. **Accessibility + dark parity:** nothing truncates at font scale 1.3; touch targets
    ≥48dp; both themes verified per screen. **Token storage decision (F17)** lands here
    too.
36. **Roborazzi baselines for every screen** light+dark (Transactions, CashEntry, SignIn,
    Settings, Budgets, Calendar are currently uncovered), including empty/error states —
    sibling coverage is the bar (Plate 24, Spotter 21, Magpie 10). `[H]` One owner eyeball
    pass on the real phone — the wrapped-money bug shipped precisely because a recorded
    baseline was accepted without a human looking at it.

## Tier 5 — Acceptance + hygiene (v1 exit)

37. **Statement parity: two consecutive months to the penny** (§9 — mostly elapsed time +
    monthly `[H]` reconciliation). The alert pipeline is officially primary only after this.
38. **Turn the AI on for real:** set `llm_base_url` to the live LM Studio, verify draft
    quality against real post-backfill data. **First insights** ship if drafts prove
    trustworthy — insights may slip post-v1 without blocking; category drafts may not.
39. **Fixture-sanitization CI guard** (§9): a test asserting every committed `.eml` uses
    only sentinel values (last4 `0000`, `SENTINEL` merchants). A real PII leak already
    happened once this build; gitleaks doesn't know what a merchant looks like.
40. **Operational loose ends:** confirm the uptime-kuma monitor exists and pages; clean the
    smoke user's residue out of prod (and document that the smoke user is expected); update
    host `C:\Code\CLAUDE.md` + ROADMAP2's Magpie rows from "PLANNED" to live (they currently
    lie about a prod service); fold this file's outcome back into ARCHITECTURE.md as-built.

## Explicitly still post-v1 (unchanged, see ROADMAP.md)

Subscription surfacing · SimpleFIN Bridge (only on its trigger conditions) · household/
multi-user · OFX beyond CSV if CSV proves sufficient · paystub photo (permanent non-goal).

<?xml version="1.0" encoding="utf-8"?>
<!-- Near-white background for the low-poly magpie foreground (2026-07-09). The bird carries the
     brand color itself (iridescent blue/teal/green facets), so it sits on a clean light ground
     rather than the old teal gradient, which its own wings blended into. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFF5F6F8"
        android:pathData="M0,0h108v108h-108z" />
</vector>
